<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:background="@android:color/white"
    android:gravity="center|top"
    android:paddingTop="30dp"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/listview_button"
        android:layout_width="250dp"
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:background="@drawable/button"
        android:textColor="@android:color/white"
        android:layout_height="50dp"
        android:text="ListView" />

    <Button
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:textColor="@android:color/white"
        android:background="@drawable/button"
        android:id="@+id/recyclerview_button"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:text="RecyclerView" />

    <Button
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:textColor="@android:color/white"
        android:background="@drawable/button"
        android:id="@+id/recyclerview_remote_button"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:text="RecyclerView-Service" />

    <Button
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:textColor="@android:color/white"
        android:background="@drawable/button"
        android:id="@+id/recyclerview_search_button"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:text="RecyclerView-search" />

    <Button
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:textColor="@android:color/white"
        android:background="@drawable/button"
        android:id="@+id/recyclerview_grid_button"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:text="RecyclerView-Grid" />
</LinearLayout>